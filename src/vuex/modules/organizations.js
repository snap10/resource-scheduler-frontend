import authApi from '../../api/auth'
import userApi from '../../api/users'
// var jwtDecode = require('jwt-decode')

// initial state
const state = {
}

// mutations
const mutations = {}

const actions = {
  
}

const getters = {

}
export default {
  state,
  mutations,
  actions,
  getters
}
